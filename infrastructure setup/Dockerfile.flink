FROM flink:1.18

# Install Python + PyFlink (exact version match)
USER root
RUN apt-get update && \
    apt-get install -y python3 python3-pip && \
    pip3 install apache-flink==1.18.0 && \
    rm -rf /var/lib/apt/lists/*

USER flink
